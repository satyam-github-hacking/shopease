version: '3'

services:
  backend:
    build: .
    ports:
      - "8000:8000"

  postgres:
    image: postgres
    environment:
      POSTGRES_DB: ecommerce
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin